{
  "name": "prompt-engineering-architect",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Prompt Engineering course module — Tailwind v3 build.",
  "scripts": {
    "dev": "tailwindcss -i ./input.css -o ./tailwind.css --watch",
    "build": "tailwindcss -i ./input.css -o ./tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "tailwindcss": "^3.4.0"
  }
}
